# 参与贡献

- 每次修改应保持范围明确，并说明修改目的。
- 不得提交病案数据、凭据或来源不明的第三方规则数据。
- 修改数据包的规范化 `raw/` 输入后，再重新生成派生文件。
- 保持诊断/手术顺序和 `matchTrace` 行为稳定。
- 行为发生变化时，应新增或更新确定性的测试用例。
- 提交前运行 `npm run check`。

新增或更新规则包时，必须提供完整的数据来源记录，并说明源数据及其生成结果的再分发依据。仅提供一个公开下载地址，不能单独证明拥有再分发权。
