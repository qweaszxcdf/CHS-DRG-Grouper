# 数据政策

## 公开仓库边界

本版本保留下列运行或数据重建实际需要的内容：

- ICD 诊断与手术编码及名称
- CC、MCC、CCE 和无效编码表
- MDC、ADRG、DRG 分组规则
- 国临码与医保码转换映射
- 规范化构建输入和运行时生成数据

完整清单见 [DATA_SOURCES.md](DATA_SOURCES.md)。参考 PDF、Excel、重复知识库、配置模板，以及应用不读取的 CSV 导出均不包含在发布版中。

文件被纳入仓库，不代表所有上游来源都允许再分发、修改或分发衍生数据。上游资料可以公开下载，也不等于自动取得再分发许可。每次公开发布前，发布者都应自行确认适用条款。

## 新增或更新数据

每个数据包应记录：

- 权利人和权威来源地址
- 准确版本、发布日期和获取日期
- 源文件 SHA-256
- 适用许可证或使用条款
- 修改、再分发和衍生数据再分发权限
- 转换命令、生成结果和署名要求

请使用 `DATA_PROVENANCE_TEMPLATE.json`。如果授权状态不明确，应明确记录不确定性，并由仓库维护者作出发布决定。

## 病案数据

不得提交真实病案、生产导出、带有身份信息的截图，或者可将诊断/手术组合关联到个人的日志。测试数据必须去标识化或完全合成，并且不得包含凭据、令牌等敏感信息。

## 许可证边界

Apache-2.0 仅覆盖项目自行编写的软件和文档，不会重新授权官方文件、ICD 描述、DRG 规则、编码表、转换映射，或包含这些来源内容的生成数据。
