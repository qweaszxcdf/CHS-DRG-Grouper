# 第三方声明

本应用通过 npm 使用第三方软件包。直接运行时依赖及其声明的许可证如下：

| 软件包 | 许可证 |
|---|---|
| `lucide-react` | ISC |
| `papaparse` | MIT |
| `react` | MIT |
| `react-dom` | MIT |
| `xlsx` | Apache-2.0 |

准确版本和传递依赖记录在 `package-lock.json` 中。各软件包仍受其自身许可证约束；重新分发构建产物时，应保留适用的许可证和声明。

仓库还包含官方或第三方 DRG/ICD 内容。这些编码、名称、规则、映射和衍生数据不属于项目自行编写软件的 Apache-2.0 授权范围。详见 `DATA_SOURCES.md` 和 `DATA_POLICY.md`。
